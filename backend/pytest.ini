[pytest]
testpaths = tests
asyncio_mode = auto
addopts = -ra -q
filterwarnings =
    ignore::DeprecationWarning
markers =
    slow: marks tests as slow (e.g. NLP evaluation runs); skip with `pytest -m 'not slow'`.
