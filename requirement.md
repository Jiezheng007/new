# T3：舆情风控管理系统架构设计决策

本系统面向网络舆情监测、风险识别、分级预警和协同处置业务。作为结课作业，系统重点体现架构风格选择、模块职责划分、核心业务流程和关键质量属性设计，不展开企业级微服务治理、复杂审批流程和大规模部署细节。

系统采用**混合体系结构**：整体以事件驱动风格为主，用于风险识别后的预警触发、消息通知、工单流转和状态更新；在数据采集与智能分析链路中采用管道-过滤器风格，将采集、清洗、去重、情感分析、风险识别和事件聚类拆分为独立处理阶段；在数据管理层采用数据为中心的仓库风格，统一管理舆情数据、分析结果、预警记录、工单、报告和审计日志。

在课程实现层面，系统可采用 **FastAPI 模块化单体** 方式落地，逻辑上体现微服务思想。数据库访问采用 **SQLAlchemy ORM**，权限控制采用 **RBAC**，事件驱动部分可通过后台任务或任务队列进行简化实现。

------

## 一、架构设计目标

舆情风控管理系统需要从新闻门户、社交平台、短视频平台、问答社区、投诉平台等多源异构渠道采集数据，并完成清洗、去重、情感分析、风险识别、预警推送、工单流转、可视化展示和报告导出等功能。

结合课程作业范围，系统架构设计目标如下：

1. **实时性**：重点舆情数据能够较快进入分析链路，高危舆情能够优先触发预警。
2. **可扩展性**：新增数据源、分析规则、预警渠道和报告模板时，不需要大规模修改核心业务逻辑。
3. **可维护性**：系统模块职责清晰，便于独立开发、测试和后续扩展。
4. **安全性**：支持用户认证、RBAC 权限控制、密码加密、输入校验和操作日志审计。
5. **可靠性**：关键业务数据持久化存储，异步任务失败后可重新处理。
6. **易用性**：提供 Web 管理端，支持舆情查看、预警确认、工单处理和报告下载。

本系统的第一优先级是：**高危舆情的实时发现与快速处置**。

------

## 二、总体架构风格选择

本系统采用：

> **以事件驱动体系结构风格为主，结合管道-过滤器体系结构风格和数据为中心体系结构风格的混合体系结构。**

其中：

| 架构风格 | 在本系统中的作用 |
| --- | --- |
| 事件驱动架构 | 用于风险命中、预警生成、消息通知、工单创建、状态变更和审计记录 |
| 管道-过滤器架构 | 用于数据采集、清洗、去重、情感分析、风险识别、事件聚类等处理流程 |
| 数据为中心架构 | 用于统一管理舆情数据、预警记录、工单、报告、权限和审计日志 |
| 分层架构 | 用于组织 Web 前端、API 接口、业务服务、数据访问和基础设施 |
| 服务化模块划分 | 在 FastAPI 项目中按业务能力拆分模块，后续可演进为独立微服务 |

------

## 三、架构设计决策一：采用事件驱动架构作为主架构

### 1. 决策内容

系统将舆情处理过程中的关键状态变化抽象为事件，例如：

```text
舆情数据采集完成事件
→ 数据清洗完成事件
→ 风险命中事件
→ 预警生成事件
→ 预警确认事件
→ 工单创建事件
→ 工单状态变更事件
→ 报告生成完成事件
```

相关模块通过事件或异步任务协作，降低模块之间的直接依赖。

### 2. 选择理由

舆情风控系统的核心业务具有明显的事件触发特征。当系统识别出高危负面舆情后，需要尽快触发预警、通知风控人员、创建工单、更新大屏并记录审计日志。如果所有操作都使用同步调用，容易造成接口阻塞，也会增加模块耦合。

事件驱动架构能够将风险识别、预警生成、工单流转和审计记录解耦，适合突发舆情场景下的异步处理。

### 3. 课程实现方式

完整工程中可以使用 Kafka、RabbitMQ、Redis Stream 等消息队列实现事件总线。课程实现中可以简化为：

```text
FastAPI BackgroundTasks
APScheduler 定时任务
Celery + Redis 任务队列
```

也就是说，架构上保留事件驱动思想，实现上根据课程复杂度选择轻量方案。

### 4. 示例流程

```text
风险识别模块发现高危舆情
        ↓
发布“风险命中事件”
        ↓
预警模块生成待确认预警
        ↓
风控人员确认预警
        ↓
工单模块创建处置工单
        ↓
审计模块记录关键操作
        ↓
可视化模块更新风险概览
```

------

## 四、架构设计决策二：数据处理链路采用管道-过滤器风格

### 1. 决策内容

舆情数据从采集到分析具有明显的连续处理特征，因此采用管道-过滤器架构。每个处理步骤封装为独立过滤器，数据按顺序经过不同处理节点。

### 2. 数据处理流水线

```text
多源数据采集
    ↓
格式标准化
    ↓
无效数据过滤
    ↓
文本去重
    ↓
情感倾向分析
    ↓
风险关键词与规则识别
    ↓
事件聚类与简单溯源
    ↓
分析结果入库
    ↓
预警规则判断
```

### 3. 高危舆情快速通道

为了保证高危舆情优先处理，系统在普通分析链路之外增加简化的高危快速通道：

```text
采集数据
    ↓
轻量清洗
    ↓
敏感关键词 / 主体名称 / 来源权重 / 负面倾向快速判断
    ↓
生成待确认预警
    ↓
后续补充完整分析结果
```

高危快速通道采用**规则优先、模型辅助**的方式进行初步判断。默认生成“待确认预警”，由风控人员确认后进入正式处置流程。

### 4. 选择理由

管道-过滤器风格的优势在于：

- 每个处理步骤职责单一；
- 便于独立开发和测试；
- 便于替换分析算法或第三方 NLP 服务；
- 便于新增数据处理节点；
- 符合文本清洗、NLP 分析和风险识别的处理特点。

课程作业中不需要自研 NLP 模型，智能分析模块可以封装已有模型或第三方文本分析接口，对外提供统一 API。

------

## 五、架构设计决策三：采用数据为中心的仓库架构

### 1. 决策内容

系统以统一数据中心作为核心数据支撑，对舆情数据、分析结果、预警记录、工单记录、报告文件、用户权限和审计日志进行统一管理。

### 2. 数据中心组成

```text
舆情数据表
分析结果表
预警记录表
工单业务表
报告任务表
用户角色权限表
操作日志审计表
```

如果系统规模扩大，可以引入全文检索引擎、对象存储和日志平台作为扩展。

### 3. FastAPI 与 ORM 数据访问

后端服务采用 FastAPI 提供 RESTful API，数据库访问采用 SQLAlchemy ORM 进行封装。

```text
FastAPI 路由层
    ↓
业务 Service 层
    ↓
Repository / ORM 数据访问层
    ↓
关系数据库
```

ORM 主要负责管理业务数据，例如用户、角色、权限、数据源配置、预警规则、预警记录、工单、报告元数据和审计日志。舆情正文和全文检索能力可以根据课程实现范围选择：

```text
简化实现：使用关系数据库文本字段存储和查询
扩展实现：接入 Elasticsearch / OpenSearch 进行全文检索
```

### 4. 数据存储建议

| 数据类型 | 建议存储方式 |
| --- | --- |
| 用户、角色、权限、工单、预警记录 | MySQL / PostgreSQL + SQLAlchemy ORM |
| 舆情正文与分析结果 | 关系数据库，必要时扩展全文检索引擎 |
| 报告文件、截图 | 本地文件存储，必要时扩展对象存储 |
| 审计日志 | 关系数据库日志表，必要时扩展日志系统 |
| 异步事件与任务 | 后台任务 / Celery + Redis，必要时扩展消息队列 |

------

## 六、架构设计决策四：采用 FastAPI 模块化单体，体现微服务思想

### 1. 决策内容

课程实现不采用完整微服务部署，而采用 **FastAPI 模块化单体**。系统按业务能力划分模块，各模块在同一后端项目中开发和运行，内部通过清晰的 Service 层和数据访问层解耦。

### 2. 模块划分

```text
用户权限模块
数据源配置模块
舆情数据模块
智能分析模块
风险预警模块
工单管理模块
报告生成模块
可视化统计模块
审计日志模块
系统配置模块
```

### 3. 选择理由

完整微服务架构需要服务注册、配置中心、链路追踪、容器编排、服务治理和分布式事务等能力，超出结课作业范围。模块化单体更适合课程实现：

- 项目结构清晰；
- 部署简单；
- 便于完成核心业务闭环；
- 仍然可以体现服务化拆分思想；
- 后续可将采集、分析、预警、报告等模块拆分为独立微服务。

### 4. 推荐项目结构

```text
app/
├── main.py
├── api/
├── core/
├── models/
├── schemas/
├── services/
├── repositories/
├── tasks/
└── modules/
    ├── auth/
    ├── sources/
    ├── opinions/
    ├── analysis/
    ├── alerts/
    ├── tickets/
    ├── reports/
    └── audit/
```

------

## 七、系统总体架构设计

系统总体架构可以分为五层：

```text
一、用户访问层
Web 管理端

二、接口接入层
FastAPI RESTful API / 用户认证 / RBAC 鉴权 / 参数校验

三、业务模块层
用户权限 / 数据源配置 / 舆情数据 / 智能分析 / 风险预警 / 工单管理 / 报告生成 / 可视化统计 / 审计日志

四、事件与任务层
后台任务 / 定时任务 / 任务队列 / 可选消息队列

五、数据存储层
关系数据库 / 可选全文检索 / 文件存储 / 日志表
```

### 架构示意图

```text
Web 管理端
    ↓
FastAPI RESTful API
    ↓
认证与 RBAC 权限校验
    ↓
业务模块层
    ├── 用户权限模块
    ├── 舆情数据模块
    ├── 智能分析模块
    ├── 风险预警模块
    ├── 工单管理模块
    ├── 报告生成模块
    └── 审计日志模块
    ↓
事件与任务层
    ├── 风险命中事件
    ├── 预警生成任务
    ├── 工单状态更新任务
    └── 报告异步生成任务
    ↓
数据存储层
    ├── 关系数据库
    ├── 可选全文检索引擎
    ├── 文件存储
    └── 审计日志表
```

------

## 八、核心业务流程设计

### 1. 舆情采集与分析流程

```text
配置数据源
    ↓
采集舆情数据
    ↓
标准化数据格式
    ↓
清洗与去重
    ↓
情感分析与风险识别
    ↓
保存分析结果
    ↓
判断是否触发预警
```

数据采集不需要真实接入所有平台，课程作业中采用“多源采集适配器 + 标准数据格式”的设计：

```text
新闻适配器
社交平台适配器
短视频平台适配器
投诉平台适配器
        ↓
统一舆情数据格式
```

### 2. 高危预警流程

```text
风险规则命中
    ↓
生成待确认预警
    ↓
风控人员确认
    ↓
形成正式预警
    ↓
可选择创建工单
    ↓
记录审计日志
```

预警等级采用可配置综合评分模型，评分因素包括：

```text
内容负面程度
数据源权重
主体重要程度
传播热度
敏感关键词命中情况
情感分析结果
人工确认结果
```

### 3. 工单流转流程

```text
正式预警
    ↓
风控人员一键创建工单
    ↓
系统推荐处置部门
    ↓
人工确认派发
    ↓
处置人员更新处理进度
    ↓
工单完成并归档
```

课程作业中不设计复杂审批和自动派发机制，重点体现预警到处置的闭环。

### 4. 报告生成流程

报告生成采用异步任务方式：

```text
用户提交报告生成请求
    ↓
系统创建报告任务，状态为“生成中”
    ↓
后台任务汇总数据并生成报告文件
    ↓
更新任务状态为“已完成”
    ↓
用户在报告中心下载报告
```

------

## 九、权限与安全设计

系统采用 RBAC 权限模型，角色设计如下：

| 角色 | 权限范围 |
| --- | --- |
| 系统管理员 | 管理用户、角色、权限、数据源配置和系统参数 |
| 风控人员 | 查看舆情数据、确认预警、创建和跟踪工单 |
| 处置人员 | 查看分配给自己的工单，更新处理进度 |
| 审计人员 | 查看操作日志和规则变更记录 |
| 普通查看人员 | 只读查看大屏、报表和公开数据 |

安全策略包括：

- 使用 HTTPS 进行传输加密；
- 用户密码采用哈希加盐存储；
- FastAPI 接口统一进行身份认证；
- 通过 RBAC 控制功能访问权限；
- 使用 Pydantic Schema 进行请求参数校验；
- 通过 ORM 减少手写 SQL 带来的注入风险；
- 对预警确认、工单派发、规则修改等关键操作记录审计日志。

------

## 十、关键质量属性设计策略

### 1. 实时性与性能

- 高危舆情进入快速处理通道；
- 风险识别后通过事件或后台任务触发预警；
- 报告生成采用异步任务，避免阻塞接口请求；
- 首页大屏展示预聚合统计数据，减少实时复杂查询。

### 2. 可扩展性

- 数据源通过适配器模式扩展；
- 分析能力通过独立分析模块封装；
- 预警规则采用可配置评分模型；
- 报告模板可独立扩展；
- 后续可将模块化单体中的高负载模块拆分为独立服务。

### 3. 可靠性

- 关键业务数据存入关系数据库；
- 异步任务记录执行状态；
- 报告任务、预警记录和工单状态可追踪；
- 任务失败后可以重新执行；
- 重要操作写入审计日志。

### 4. 安全性

- 用户认证和 RBAC 鉴权；
- 密码加密存储；
- 参数校验和 ORM 数据访问；
- 敏感操作审计；
- 规则修改、预警确认、工单流转均保留操作记录。

### 5. 易用性

- 只设计 Web 管理端；
- 首页展示风险概览、预警列表、热点趋势和待处理工单；
- 风险等级使用颜色区分；
- 支持预警确认、一键创建工单和报告下载；
- 提供基础筛选、搜索和状态跟踪能力。

------

## 十一、技术方案建议

| 层次 | 建议技术 |
| --- | --- |
| Web 后端 | FastAPI |
| 数据校验 | Pydantic |
| ORM | SQLAlchemy |
| 数据库迁移 | Alembic |
| 关系数据库 | MySQL / PostgreSQL |
| 异步任务 | FastAPI BackgroundTasks / Celery + Redis |
| 全文检索 | 可选 Elasticsearch / OpenSearch |
| 文件存储 | 本地文件存储，后续可扩展对象存储 |
| 前端 | Vue / React / 其他 Web 前端框架 |

上述技术为建议方案，不强制绑定。课程实现可以优先完成 FastAPI、ORM、关系数据库和 Web 管理端的核心闭环。

------

## 十二、主要架构设计决策汇总表

| 决策编号 | 架构决策 | 解决的问题 | 影响的质量属性 |
| --- | --- | --- | --- |
| AD-01 | 采用事件驱动架构作为主架构 | 支持预警、工单、报告和审计等异步流程 | 实时性、可维护性 |
| AD-02 | 数据处理链路采用管道-过滤器风格 | 支持采集、清洗、分析、识别的流水线处理 | 可维护性、可扩展性 |
| AD-03 | 增加高危舆情快速通道 | 保证高危风险优先发现和预警 | 实时性 |
| AD-04 | 采用数据为中心的仓库架构 | 统一管理舆情、预警、工单、报告和日志 | 一致性、可追溯性 |
| AD-05 | 采用 FastAPI 模块化单体 | 降低课程实现复杂度，同时保留模块化设计 | 可维护性、可实现性 |
| AD-06 | 使用 SQLAlchemy ORM | 统一数据访问，减少手写 SQL 风险 | 安全性、可维护性 |
| AD-07 | 采用 RBAC 权限模型 | 控制不同角色的功能访问权限 | 安全性 |
| AD-08 | 报告生成异步化 | 避免耗时任务阻塞用户请求 | 性能、可用性 |
| AD-09 | 全文检索作为可选扩展 | 支持大规模舆情文本搜索 | 性能、扩展性 |
| AD-10 | 关键操作审计日志 | 支持问题追踪和责任追溯 | 安全性、可追溯性 |

------

## 十三、结论

本系统采用事件驱动、管道-过滤器和数据为中心相结合的混合架构，能够较好地支撑舆情采集、风险识别、分级预警、工单处置、报告生成和审计追踪等核心业务。

考虑到本项目是结课作业，系统实现不采用完整企业级微服务架构，而是使用 FastAPI 模块化单体方式落地，并通过 SQLAlchemy ORM、RBAC 权限控制、异步任务和可扩展模块划分体现工程化设计思想。该方案既能满足课程设计对架构合理性的要求，又能控制实现复杂度，便于完成可运行的系统原型。
