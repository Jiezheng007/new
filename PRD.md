# 舆情风控管理系统 PRD

## 1. 文档说明

本文档面向“舆情风控管理系统”的实际编码实现，用于指导课程作业中的 MVP 原型开发。系统需要能够运行、演示核心业务闭环，并与 `requirement.md` 中的体系结构设计保持一致。

系统实现重点不是构建企业级舆情平台，而是在可控范围内完成：

- 公开新闻/RSS 数据接入；
- 现成 NLP 模型或服务调用；
- 风险识别与分级预警；
- 预警确认与工单处置；
- 报告生成与统计展示；
- RBAC 权限控制与审计日志。

## 2. 产品背景

网络舆情来源分散、传播速度快、风险变化明显。组织需要及时发现与自身相关的负面信息，对高风险内容进行预警，并推动责任人员完成处置。

本系统面向网络舆情监测、风险识别、分级预警和协同处置场景，提供一个可运行的 Web 管理端和后端服务，实现从数据接入到风险处置的闭环。

## 3. 产品目标

### 3.1 核心目标

系统第一优先级是：高危舆情的及时发现与快速处置。

### 3.2 MVP 目标

- 支持接入公开 RSS/新闻源，并支持手动导入 CSV/JSON 数据作为演示兜底。
- 支持调用现成中文 NLP 模型或第三方 NLP 服务完成情感分析。
- 支持基于情感分析、关键词、来源权重、热度和主体命中的规则评分。
- 支持自动生成待确认预警，并由风控人员确认、忽略或转工单。
- 支持工单派发、处理、完成和归档。
- 支持 Excel 报告异步生成与下载，PDF 报告作为选做。
- 支持五类用户角色和基础 RBAC 权限控制。
- 支持关键操作审计日志。

## 4. 用户角色

| 角色 | 主要职责 |
| --- | --- |
| 系统管理员 | 管理用户、角色、权限、数据源、风险规则和系统参数 |
| 风控人员 | 查看舆情、确认预警、忽略预警、创建工单、跟踪工单、生成报告 |
| 处置人员 | 查看分配给自己的工单，更新处理进度和处置结果 |
| 审计人员 | 查看操作日志、规则变更记录、预警确认记录和工单流转记录 |
| 普通查看人员 | 只读查看工作台、舆情列表、统计图表和报告结果 |

## 5. 范围定义

### 5.1 必做范围

- FastAPI 后端服务。
- SQLAlchemy ORM 数据访问。
- 关系数据库存储核心业务数据。
- Web 管理端。
- 登录认证与 RBAC 鉴权。
- 公开 RSS/新闻源数据接入。
- CSV/JSON 手动导入。
- 舆情数据清洗、去重、标准化。
- 现成 NLP 情感分析模型或服务调用。
- 规则评分与风险等级计算。
- 待确认预警生成。
- 预警确认、忽略、转工单。
- 工单派发、处理、完成、归档。
- Excel 报告异步生成与下载。
- 工作台统计展示。
- 审计日志。

### 5.2 选做范围

- PDF 报告导出。
- 相似文本归并或简单事件聚类。
- Elasticsearch / OpenSearch 全文检索。
- Celery + Redis 任务队列。
- 更多数据源适配器。
- 报告模板扩展。

### 5.3 不做范围

- 不自研 NLP 模型。
- 不实现复杂多级审批流。
- 不实现大规模分布式爬虫。
- 不绕过登录、反爬或非公开接口抓取数据。
- 不承诺秒级流式采集。
- 不实现完整企业级微服务治理。
- 不实现复杂舆情溯源和传播路径推断。
- 不做报告模板编辑器。

## 6. 核心业务流程

### 6.1 数据接入与分析流程

```text
配置 RSS/新闻数据源
    ↓
手动拉取或定时拉取
    ↓
标准化为统一舆情数据格式
    ↓
清洗、去重、入库
    ↓
调用 NLP Provider 完成情感分析
    ↓
执行关键词、主体、来源权重、热度规则
    ↓
计算风险评分和风险等级
    ↓
保存分析结果
    ↓
判断是否生成待确认预警
```

### 6.2 高危预警流程

```text
舆情数据入库
    ↓
高危快速规则判断
    ↓
命中高危条件
    ↓
生成待确认预警
    ↓
风控人员确认或忽略
    ↓
确认后可创建工单
```

高危快速判断采用规则优先、模型辅助方式。规则因素包括敏感关键词、主体命中、来源权重、负面情感和热度指标。

### 6.3 工单流转流程

```text
已确认预警
    ↓
风控人员创建工单
    ↓
选择处置人员并派发
    ↓
处置人员更新为处理中
    ↓
处置人员提交处理结果并标记完成
    ↓
风控人员或系统归档
```

工单状态包括：

- 待派发
- 处理中
- 已完成
- 已归档

### 6.4 报告生成流程

```text
用户选择报告条件
    ↓
提交报告生成任务
    ↓
系统创建报告任务，状态为生成中
    ↓
后台任务汇总数据并生成 Excel 文件
    ↓
更新报告任务为已完成或失败
    ↓
用户下载报告
```

报告任务状态包括：

- 生成中
- 已完成
- 失败

## 7. 功能需求

### 7.1 登录与权限

系统应支持用户登录、退出和身份认证。后端接口应基于当前用户角色进行 RBAC 鉴权。

功能点：

- 用户登录。
- 用户退出。
- 获取当前用户信息。
- 根据角色控制菜单和接口访问。
- 未登录用户不能访问业务接口。
- 无权限用户访问接口时返回权限错误。

验收标准：

- 不同角色登录后只能看到自己有权限访问的功能。
- 无权限接口调用被拒绝。
- 用户密码不得明文存储。

### 7.2 用户与角色管理

系统管理员可以维护用户、角色和权限。

功能点：

- 用户列表。
- 新增、编辑、禁用用户。
- 重置用户密码。
- 分配用户角色。
- 查看角色权限。

验收标准：

- 管理员可以创建五类角色用户。
- 被禁用用户不能登录。
- 普通用户不能访问用户管理功能。

### 7.3 数据源管理

系统支持配置公开 RSS/新闻数据源。

功能点：

- 新增 RSS/新闻数据源。
- 编辑数据源名称、URL、来源类型、来源权重。
- 启用或停用数据源。
- 手动触发拉取。
- 查看最近拉取时间和拉取结果。

验收标准：

- 系统可以从至少一个公开 RSS/新闻源拉取数据。
- 手动拉取后可以在舆情列表看到新增数据。
- 停用数据源后定时任务不再拉取该数据源。

### 7.4 数据导入

系统支持 CSV/JSON 手动导入，用于课程展示和兜底演示。

功能点：

- 上传 CSV 或 JSON 文件。
- 校验字段格式。
- 导入合法数据。
- 展示导入成功、失败数量。

验收标准：

- 合法文件可以导入舆情数据。
- 缺少必要字段的数据不能导入，并返回错误原因。

### 7.5 舆情数据管理

系统提供舆情列表和详情查看能力。

功能点：

- 舆情列表分页。
- 按时间、来源、关键词、风险等级、情感倾向筛选。
- 查看舆情标题、正文摘要、来源、发布时间、采集时间。
- 查看分析结果、风险评分、关联预警。

验收标准：

- 用户可以按条件搜索舆情。
- 舆情详情中可以看到 NLP 分析结果和风险评分说明。

### 7.6 NLP 分析

系统调用现成中文 NLP 模型或第三方服务完成情感分析。实现上应封装为可替换的 NLP Provider。

功能点：

- 对舆情标题和正文进行情感分析。
- 输出正向、中性、负向。
- 输出置信度。
- 保存分析结果。
- NLP 调用失败时记录失败状态和错误信息。

验收标准：

- 新增舆情数据后可以生成情感分析结果。
- NLP 服务不可用时不影响舆情数据入库。
- 失败记录可以被重新分析。

### 7.7 风险规则与评分

系统基于规则评分模型计算风险等级。

评分因素：

- 情感分析结果。
- 敏感关键词命中。
- 主体关键词命中。
- 来源权重。
- 热度指标。
- 人工确认结果。

风险等级：

- 低风险
- 中风险
- 高风险
- 严重风险

功能点：

- 管理员维护敏感关键词。
- 管理员维护主体关键词。
- 管理员维护风险等级阈值。
- 系统计算风险分数。
- 系统保存评分明细。

验收标准：

- 命中高危规则的数据能自动生成待确认预警。
- 风险评分结果可在舆情详情中查看。
- 修改规则后，新数据按新规则计算。

### 7.8 预警中心

系统根据风险规则生成预警，风控人员可以处理预警。

预警状态：

- 待确认
- 已确认
- 已忽略
- 已转工单

功能点：

- 查看预警列表。
- 按状态、等级、时间、关键词筛选。
- 查看预警详情。
- 确认预警。
- 忽略预警并填写原因。
- 将已确认预警创建为工单。

验收标准：

- 高风险或严重风险舆情可以生成待确认预警。
- 风控人员可以确认或忽略预警。
- 预警状态变化会写入审计日志。

### 7.9 工单管理

系统支持轻量闭环工单流转。

功能点：

- 从已确认预警创建工单。
- 风控人员选择处置人员并派发。
- 处置人员查看自己的工单。
- 处置人员更新处理进度。
- 处置人员提交处理结果。
- 风控人员或系统归档已完成工单。

验收标准：

- 风控人员可以将预警转为工单。
- 处置人员只能看到分配给自己的工单。
- 工单状态变化会写入审计日志。

### 7.10 工作台与统计

系统首页为后台管理式工作台。

功能点：

- 展示舆情总量。
- 展示负面舆情数量和占比。
- 展示高风险/严重风险预警数量。
- 展示待确认预警数量。
- 展示待处理工单数量。
- 展示近 7 日舆情趋势。
- 展示最新预警列表。

验收标准：

- 登录后首屏可以看到系统核心风险概览。
- 统计数据与数据库中的业务数据一致。

### 7.11 报告中心

系统支持生成和下载 Excel 报告。

功能点：

- 选择时间范围。
- 选择风险等级。
- 输入主体关键词。
- 创建报告任务。
- 查看报告任务列表。
- 下载已完成 Excel 报告。
- 查看失败原因。

报告内容：

- 舆情总量。
- 负面舆情占比。
- 高危预警列表。
- 工单处理情况。
- 热点关键词。
- 趋势统计。

验收标准：

- 用户可以创建报告任务。
- 报告生成过程不阻塞接口请求。
- 已完成报告可以下载。

### 7.12 审计日志

系统记录关键操作。

审计范围：

- 登录成功和失败。
- 用户和角色变更。
- 数据源变更。
- 风险规则变更。
- 预警确认、忽略和转工单。
- 工单派发和状态变更。
- 报告生成和下载。

验收标准：

- 审计人员可以查看审计日志。
- 普通查看人员不能访问审计日志。
- 日志中包含操作人、操作时间、操作对象和操作结果。

## 8. 页面需求

MVP Web 管理端页面包括：

| 页面 | 主要内容 |
| --- | --- |
| 登录页 | 用户名、密码、登录按钮、错误提示 |
| 工作台 | 风险概览、趋势统计、最新预警、待处理工单 |
| 数据源管理 | 数据源列表、新增、编辑、启停、手动拉取 |
| 数据导入 | CSV/JSON 上传、导入结果 |
| 舆情列表 | 搜索、筛选、分页、详情入口 |
| 舆情详情 | 原文、来源、分析结果、风险评分、关联预警 |
| 预警中心 | 预警列表、确认、忽略、转工单 |
| 工单管理 | 工单列表、派发、更新进度、完成、归档 |
| 报告中心 | 创建报告任务、任务状态、下载 |
| 审计日志 | 操作日志查询 |
| 用户管理 | 用户列表、新增、编辑、禁用、分配角色 |
| 规则管理 | 敏感词、主体词、阈值和来源权重配置 |

## 9. 核心数据模型

### 9.1 User

- id
- username
- password_hash
- display_name
- role_id
- status
- created_at
- updated_at

### 9.2 Role

- id
- name
- code
- description

### 9.3 Permission

- id
- code
- name
- description

### 9.4 DataSource

- id
- name
- source_type
- url
- weight
- status
- last_fetch_at
- last_fetch_status
- created_by
- created_at

### 9.5 OpinionItem

- id
- title
- content
- url
- source_id
- source_name
- source_type
- published_at
- collected_at
- author
- heat
- content_hash
- status

### 9.6 AnalysisResult

- id
- opinion_id
- sentiment
- sentiment_score
- keywords
- matched_subjects
- matched_sensitive_words
- risk_score
- risk_level
- provider_name
- analysis_status
- error_message
- created_at

### 9.7 RiskRule

- id
- rule_type
- name
- value
- weight
- status
- created_by
- updated_at

### 9.8 Alert

- id
- opinion_id
- risk_level
- risk_score
- status
- reason
- confirmed_by
- confirmed_at
- ignored_reason
- created_at

### 9.9 Ticket

- id
- alert_id
- title
- description
- assignee_id
- status
- priority
- process_result
- assigned_at
- completed_at
- archived_at
- created_by
- created_at

### 9.10 ReportTask

- id
- name
- report_type
- filters
- status
- file_path
- error_message
- created_by
- created_at
- completed_at

### 9.11 AuditLog

- id
- actor_id
- action
- target_type
- target_id
- detail
- ip_address
- result
- created_at

## 10. 非功能需求

### 10.1 实时性

- 系统支持手动拉取数据源。
- 系统支持定时拉取数据源。
- 默认采集周期为 5-15 分钟。
- 高危内容入库后应立即进入快速风险判断。

### 10.2 性能

- 列表页面应支持分页查询。
- 报告生成应异步执行。
- 工作台统计可使用预聚合或轻量查询。
- MVP 不要求支撑大规模并发。

### 10.3 可扩展性

- 数据源接入采用适配器方式。
- NLP 调用封装为 Provider 接口。
- 风险规则可配置。
- 报告生成逻辑与业务查询解耦。
- 后续可将采集、分析、预警、报告模块拆分为独立服务。

### 10.4 可靠性

- 关键业务数据必须持久化。
- NLP 分析失败不应导致数据丢失。
- 报告任务失败应记录错误原因。
- 异步任务应记录执行状态。

### 10.5 安全性

- 密码哈希加盐存储。
- 接口统一认证。
- 基于 RBAC 控制接口权限。
- 请求参数使用 Schema 校验。
- 使用 ORM 降低 SQL 注入风险。
- 关键操作记录审计日志。

### 10.6 易用性

- 系统采用后台管理式 Web 界面。
- 首屏展示核心风险状态。
- 风险等级使用明显视觉区分。
- 常用操作应在列表页可直接完成。
- 表格支持搜索、筛选和分页。

## 11. 技术约束

推荐技术栈：

| 层次 | 技术 |
| --- | --- |
| 后端框架 | FastAPI |
| 数据校验 | Pydantic |
| ORM | SQLAlchemy |
| 数据库迁移 | Alembic |
| 关系数据库 | PostgreSQL 或 MySQL |
| 异步任务 | FastAPI BackgroundTasks / APScheduler / Celery + Redis |
| NLP | 本地开源模型或第三方 NLP API |
| 报告 | openpyxl / pandas，PDF 作为选做 |
| 前端 | Vue 或 React |

NLP 实现不强绑定供应商。系统应通过 `NlpProvider` 风格接口屏蔽具体模型或服务，便于在本地模型和第三方 API 之间切换。

## 12. MVP 验收标准

系统完成后，应至少满足以下验收条件：

- 可以登录系统，并根据不同角色展示不同权限。
- 管理员可以配置一个公开 RSS/新闻源。
- 系统可以从该数据源拉取舆情数据。
- 系统可以导入 CSV/JSON 舆情数据。
- 新增舆情数据可以触发 NLP 情感分析。
- 系统可以根据规则计算风险评分和风险等级。
- 高风险或严重风险舆情可以生成待确认预警。
- 风控人员可以确认或忽略预警。
- 风控人员可以将已确认预警创建为工单。
- 处置人员可以更新分配给自己的工单。
- 工作台可以展示舆情、预警、工单统计。
- 用户可以创建 Excel 报告任务并下载报告。
- 审计人员可以查看关键操作日志。

## 13. 里程碑建议

### M1：基础框架与权限

- 初始化 FastAPI 项目。
- 建立数据库连接和 ORM 基础模型。
- 实现用户、角色、权限。
- 实现登录认证和 RBAC 鉴权。

### M2：数据接入与舆情管理

- 实现数据源管理。
- 实现 RSS/新闻源拉取。
- 实现 CSV/JSON 导入。
- 实现舆情列表和详情。

### M3：分析与预警

- 封装 NLP Provider。
- 实现情感分析。
- 实现风险规则和评分。
- 实现预警生成、确认和忽略。

### M4：工单与审计

- 实现工单创建、派发、处理和归档。
- 实现关键操作审计日志。
- 完成角色权限校验。

### M5：报告与展示

- 实现工作台统计。
- 实现报告任务。
- 实现 Excel 报告生成和下载。
- 完成前后端联调和演示数据准备。

## 14. 风险与应对

| 风险 | 应对 |
| --- | --- |
| 公开数据源不稳定 | 保留 CSV/JSON 导入作为演示兜底 |
| NLP 模型下载或调用失败 | 抽象 Provider，准备本地规则降级方案 |
| 功能范围过大 | 优先完成数据接入、分析、预警、工单、报告闭环 |
| 权限复杂度增加 | MVP 保留五类角色，但权限点控制在核心页面和接口 |
| 报告生成耗时 | 使用异步任务，报告状态可追踪 |

## 15. 与架构设计的对应关系

| PRD 需求 | 对应架构设计 |
| --- | --- |
| 数据接入、清洗、分析 | 管道-过滤器架构 |
| 风险命中、预警生成、工单流转 | 事件驱动架构 |
| 舆情、预警、工单、报告、审计统一存储 | 数据为中心架构 |
| FastAPI 后端和业务模块划分 | 模块化单体 |
| 用户角色和权限 | RBAC |
| 报告任务和 NLP 分析 | 异步任务 |
| 关键操作记录 | 审计日志 |
